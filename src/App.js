import {useState, useEffect} from 'react'

function App() {
  // use state for form
  // use state for timer
  // use state to maintain boolean

  // useEffect to fetch the image from api

  return (
    <main>

      <div>
        {/*
          select to choose time //handle change
          select to choose type of img //handle change
          submit button //handle submit
        */}
      </div>
      {/*
        we need conditional rendring so whever it shows image form is hidden
      */}
    </main>
  );
}

export default App;
